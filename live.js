const D=window.MAKERS_DATA,Y=window.MAKERS_2026,Q=new URLSearchParams(location.search);
const e=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const navLinks=[['weeks.html','Write-Ups'],['season-2026.html','2026'],['schedule.html','Schedule'],['trophy-case.html','Trophies'],['history.html','History'],['franchises.html','Franchises'],['head-to-head.html','H2H'],['drafts.html','Draft'],['transactions.html','Moves'],['waiver-wire.html','Waivers'],['keepers.html','Keepers'],['power-rankings.html','Power'],['playoff-odds.html','Odds'],['records.html','Records'],['wall-of-shame.html','Shame']];
function nav(){return `<header class="topbar"><div class="wrap"><a class="brand" href="index.html"><span class="brandmark">M</span><span>THE MAKERS</span></a><button class="hamb" aria-label="Open navigation">☰</button><nav class="nav">${navLinks.map(x=>`<a href="${x[0]}">${x[1]}</a>`).join('')}</nav></div></header>`}
function foot(){return `<footer class="footer"><div class="wrap"><div><strong>The Makers</strong><div class="small">Fantasy football, since 2022.</div></div><div class="small">Updated: ${e(Y.lastUpdated)} · Bad decisions, keeper math, real grudges.</div></div></footer>`}
function hero(t,c='',ey='THE MAKERS · SINCE 2022'){return `<section class="pagehead"><div class="wrap"><div class="eyebrow">${e(ey)}</div><h1>${e(t)}</h1><p>${e(c)}</p></div></section>`}
function sec(t,h,i='',ey=''){return `<section class="section"><div class="wrap"><div class="section-head"><div>${ey?`<div class="kicker">${e(ey)}</div>`:''}<h2>${e(t)}</h2></div>${i?`<p class="section-intro">${e(i)}</p>`:''}</div>${h}</div></section>`}
function card(t,b,ey=''){return `<div class="card">${ey?`<div class="kicker">${e(ey)}</div>`:''}<h3>${e(t)}</h3><p>${b}</p></div>`}
function gradeClass(g){let x=(g||'').toLowerCase()[0];return ['a','b','c','d','f'].includes(x)?x:'b'}
function currentFranchise(manager){return D.franchises.find(f=>f.manager===manager)}
function historicalRecord(manager){return D.records.find(r=>r.manager===manager)}
function ourTeam(manager){return (Y.powerRankings||Y.draftRankings).find(t=>t.manager===manager)}
function teamRankCards(){return (Y.powerRankings||Y.draftRankings).map(t=>`<a class="rank-card" href="franchise.html?manager=${encodeURIComponent(t.manager)}"><div class="rank-num">${t.rank}</div><div class="rank-team"><strong>${e(t.team)}</strong><small>${e(t.manager)} · Our ${e(t.grade)}</small></div><div class="rank-grade"><strong>${e(t.grade)}</strong><small>Yahoo ${e(t.yahoo)}</small></div></a>`).join('')}
function publishedWeeks(){return Object.entries(Y.weekly||{}).map(([w,d])=>({week:Number(w),...d})).filter(d=>d.writeup&&d.writeup.trim()).sort((a,b)=>b.week-a.week)}
function homepageWeeklies(){const p=publishedWeeks();if(!p.length)return `<section class="section home-weekly-section"><div class="wrap"><div class="section-head"><div><div class="kicker">FROM THE LEAGUE DESK</div><h2>Weekly write-ups are armed</h2></div><p class="section-intro">Once Week 1 is published, the newest recap automatically takes over this spot.</p></div><div class="notice"><b>Preseason mode.</b> Every Week 1–17 route is live; the copy, results, awards, power rankings and shame watch all pull from one central season file.</div></div></section>`;
 const latest=p[0],older=p.slice(1,4);return `<section class="section home-weekly-section"><div class="wrap"><div class="section-head"><div><div class="kicker">FROM THE LEAGUE DESK</div><h2>Latest Weekly Write-Up</h2></div><p class="section-intro">The newest recap lives here automatically and remains archived on its Week page.</p></div><article class="home-weekly-feature"><div class="home-weekly-copy"><div class="home-weekly-meta"><span>WEEK ${latest.week}</span><span>${e((latest.status||'WRITE-UP').toUpperCase())}</span></div><h3>${e(latest.headline)}</h3><p>${e(latest.writeup)}</p><div class="home-weekly-actions"><a class="btn primary" href="week.html?week=${latest.week}">Open Week ${latest.week}</a><a class="home-archive-link" href="weeks.html">All write-ups →</a></div></div><aside class="home-weekly-side"><div class="kicker">GAME OF THE WEEK</div><h4>${e(latest.gameOfWeek||'To be earned')}</h4><p>${e(latest.gotwCopy||'')}</p><div class="home-weekly-side-rule"></div><div class="kicker">SHAME WATCH</div><p>${e(latest.shameWatch||'Pending.')}</p></aside></article>${older.length?`<div class="home-weekly-recent"><div class="kicker">RECENT WEEKS</div><div class="home-weekly-recent-grid">${older.map(d=>`<a class="home-weekly-mini" href="week.html?week=${d.week}"><small>WEEK ${d.week}</small><strong>${e(d.headline)}</strong><span>Read →</span></a>`).join('')}</div></div>`:''}</div></section>`}
function home(){let champs=Object.entries(D.seasons).map(([y,s])=>card(`${y} — ${s.champion}`,`<b>${e(s.champManager)}</b><br><span class="small">Runner-up: ${e(s.runnerManager)} · ${e(s.runnerUp)}</span>`,'CHAMPION')).join('');let leader=Y.draftRankings[0];return `<section class="hero"><div class="wrap"><div class="eyebrow">2026 SEASON HUB</div><h1>The Makers<br><em style="color:var(--gold);font-weight:400">League Archive</em></h1><p>Five seasons of keepers, bad beats, frantic waiver claims and permanent receipts — now running on one live 2026 season file.</p><div class="hero-actions"><a class="btn primary" href="drafts.html">Our 2026 Draft Verdict</a><a class="btn ghost" href="weeks.html">Weekly Write-Ups</a><a class="btn ghost" href="season-2026.html">2026 War Room</a></div></div></section>`+
sec('The story so far',`<div class="grid four">${champs}</div>`,'Four completed championships. 2026 starts from here.','THE TROPHY SHELF')+homepageWeeklies()+
`<section class="section alt"><div class="wrap"><div class="section-head"><div><div class="kicker">OUR PRESEASON BOARD</div><h2>Not Yahoo's rankings</h2></div><p class="section-intro">We graded roster construction, keeper value, positional balance, depth and the price paid.</p></div><div class="notice red"><b>Draft winner: ${e(leader.manager)} — ${e(leader.team)} (${e(leader.grade)}).</b> Yahoo gave this roster ${e(leader.yahoo)}. We did not.</div>${teamRankCards()}</div></section>`}
function weeks(){let h='';for(let w=1;w<=17;w++){let d=Y.weekly[String(w)]||{};h+=`<a class="week-card${w===Y.week?' current':''}" href="week.html?week=${w}"><div class="week-status">${e(d.status||'Scheduled')}</div><b>Week ${w}</b><p>${e(d.headline||'Write-up pending')}</p></a>`}return hero('2026 Weekly Write-Ups','One season file feeds every preview, recap, award, ranking and shame-watch page.')+sec('Every week gets receipts',`<div class="week-grid">${h}</div>`,'Weeks 15–17 become playoff pages once the bracket exists.','THE ARCHIVE')}
function previewFor(w,a,b){let arr=(D.weeklyPreview||{})[String(w)]||[];return arr.find(x=>(x.a===a&&x.b===b)||(x.a===b&&x.b===a))}
function week(){let w=Number(Q.get('week')||1),d=Y.weekly[String(w)]||{};let ms=(d.matchups||[]).map(m=>{let p=previewFor(w,m[0],m[1]);return `<div class="matchup"><div class="team"><b>${e(m[0])}</b>${p?`<div class="edge">${e(p.favorite===m[0]?`${p.prob}% model favorite`:`${(100-p.prob).toFixed(1)}% model chance`)}</div>`:''}</div><div class="versus">VS</div><div class="team"><b>${e(m[1])}</b>${p?`<div class="edge">${e(p.favorite===m[1]?`${p.prob}% model favorite`:`${(100-p.prob).toFixed(1)}% model chance`)}</div>`:''}</div></div>`}).join('')||'<div class="notice">Playoff matchups TBD.</div>';let aw=(d.awards||[]).length?d.awards.map(a=>card(a[1],e(a[2]||''),a[0])).join(''):card('Awards pending','The week has to earn them first.','WEEKLY HARDWARE');let write=d.writeup?`<p class="writeup-copy">${e(d.writeup)}</p>`:`<div class="empty-state"><b>Write-up pending.</b><br>This page is wired to season-2026.js; when the recap is added there, it appears here and on the homepage automatically.</div>`;return hero(`Week ${w} — 2026`,d.headline||'Write-up pending')+`<section class="section"><div class="wrap grid two"><div><div class="kicker">THE WRITE-UP</div><h2>${e(d.headline||'Pending')}</h2>${write}</div><div class="gotw"><div class="kicker">GAME OF THE WEEK</div><h3>${e(d.gameOfWeek||'To be selected')}</h3><p>${e(d.gotwCopy||'The marquee matchup will be added with the weekly preview.')}</p></div></div></section>`+sec('The slate',`<div class="grid two">${ms}</div>`)+`<section class="section alt"><div class="wrap grid three">${card('Standings movement',e(d.standingsMovement||'Pending.'),'THE TABLE')}${card('Who smells trouble?',e(d.shameWatch||'Pending.'),'SHAME WATCH')}${card('Quote of the week',e(d.quote||'Pending.'),'GROUP CHAT')}</div></section>`+sec('Weekly awards',`<div class="grid three">${aw}</div>`)+`<section class="section"><div class="wrap"><div class="kicker">CURRENT POWER</div><h2>Our rankings</h2>${teamRankCards()}</div></section>`}
function power(){let rows=(Y.powerRankings||Y.draftRankings).map(t=>`<tr><td><b>${t.rank}</b></td><td><b>${e(t.team)}</b><div class="small">${e(t.manager)}</div></td><td><span class="grade ${gradeClass(t.grade)}">${e(t.grade)}</span></td><td>${e(t.yahoo)}</td><td>${e(t.yahooProjection)}</td><td>${e(t.bestValue)}</td><td>${e(t.outlook)}</td></tr>`).join('');let cards=(Y.powerRankings||Y.draftRankings).map(t=>`<div class="card"><div class="rankline"><span class="rank-badge">#${t.rank}</span><span class="grade ${gradeClass(t.grade)}">${e(t.grade)}</span></div><div class="kicker">${e(t.manager)} · YAHOO ${e(t.yahoo)}</div><h3>${e(t.team)}</h3><p><b>The good:</b> ${e(t.best)}</p><p><b>The worry:</b> ${e(t.concern)}</p></div>`).join('');return hero('Power Rankings','Our board, not Yahoo’s. Keeper price, depth, roster completeness and actual draft construction all count.')+sec('The current pecking order',`<div class="table-wrap"><table><thead><tr><th>#</th><th>Team</th><th>Our grade</th><th>Yahoo</th><th>Yahoo projection</th><th>Best value</th><th>Verdict</th></tr></thead><tbody>${rows}</tbody></table></div>`,'Yahoo is shown for comparison, not as the final word.','OUR 2026 BOARD')+sec('Why we ranked them this way',`<div class="grid two">${cards}</div>`) }
function odds(){let o=[...(Y.modelOdds||[])].sort((a,b)=>b.playoff-a.playoff);let bars=(field)=>o.map(x=>`<div class="odds-row"><strong>${e(x.team)}</strong><div class="bar"><span style="width:${Math.min(100,x[field])}%"></span></div><b>${e(x[field])}%</b></div>`).join('');let rows=o.map((x,i)=>`<tr><td>${i+1}</td><td><b>${e(x.team)}</b><div class="small">${e(x.manager)}</div></td><td>${x.playoff}%</td><td>${x.bye}%</td><td>${x.title}%</td><td>${x.avgSeed}</td></tr>`).join('');return hero('Playoff Odds','Preseason simulation model. These are Makers model probabilities, not Yahoo promises.')+sec('Make the six-team bracket',bars('playoff'),'The model uses the preseason team-strength baseline and the actual 2026 schedule.','MODEL BOARD')+sec('Full table',`<div class="table-wrap"><table><thead><tr><th>#</th><th>Team</th><th>Playoffs</th><th>Bye</th><th>Title</th><th>Avg seed</th></tr></thead><tbody>${rows}</tbody></table></div>`)}
function seasonMove(manager,year){if(year===2026){let s=Y.standings.find(x=>x.manager===manager);return s?s.moves:null}let season=D.seasons[String(year)];if(!season)return null;let r=season.standings.find(x=>x[1]===manager);return r?r[5]:null}
function transactions(){let managers=D.records.map(r=>r.manager).filter(m=>m!=='El Rubio').concat(['El Rubio']).filter((x,i,a)=>a.indexOf(x)===i);let rows=managers.map(m=>{let vals=[2022,2023,2024,2025,2026].map(y=>seasonMove(m,y));let total=vals.reduce((s,v)=>s+(Number(v)||0),0);return `<tr><td><b>${e(m)}</b></td>${vals.map(v=>`<td>${v==null?'—':v}</td>`).join('')}<td><b>${total}</b></td></tr>`}).join('');let live=Y.recentTransactions.length?Y.recentTransactions.map(x=>`<div class="activity-card"><div><span class="activity-type">${e(x.type||'MOVE')}</span><strong>${e(x.manager||x.team||'')}</strong></div><p>${e(x.description||'')}</p><small>${e(x.time||'')}</small></div>`).join(''):`<div class="empty-state"><b>No 2026 moves yet.</b><br>The transaction tape will populate from the same season file or the Yahoo bridge.</div>`;return hero('Moves & Trades','Historical churn plus the live 2026 transaction tape.')+sec('Career move ledger',`<div class="table-wrap"><table><thead><tr><th>Manager</th><th>2022</th><th>2023</th><th>2024</th><th>2025</th><th>2026</th><th>Total</th></tr></thead><tbody>${rows}</tbody></table></div>`,'2026 updates automatically from season-2026.js.','THE CHURN')+sec('2026 activity',`<div class="activity-feed" id="transaction-feed">${live}</div>`)}
function waiverContent(w){const faab=(w.faab||Y.faab||[]).sort((a,b)=>a.priority-b.priority).map(x=>`<tr><td><b>${e(x.priority)}</b></td><td>${e(x.manager)}</td><td>${e(x.team)}</td><td>$${e(x.spent)}</td><td><b>$${e(x.remaining)}</b></td><td>${e(x.claimsWon)}</td></tr>`).join('');const avail=(w.topAvailable||Y.topAvailable||[]).length?(w.topAvailable||Y.topAvailable).map((p,i)=>`<tr><td>${i+1}</td><td><b>${e(p.name)}</b></td><td>${e(p.position||'')}</td><td>${e(p.nflTeam||'')}</td><td>${e(p.status||'FA')}</td><td>${e(p.faabSuggestion||'—')}</td></tr>`).join(''):`<tr><td colspan="6"><div class="empty-state"><b>Waiting for Yahoo availability data.</b><br>Once the secure bridge is connected, this table can populate without exposing private credentials.</div></td></tr>`;const tx=(w.recentTransactions||Y.recentTransactions||[]).length?(w.recentTransactions||Y.recentTransactions).map(x=>`<div class="activity-card"><div><span class="activity-type">${e(x.type||'MOVE')}</span><strong>${e(x.manager||x.team||'')}</strong></div><p>${e(x.description||'')}</p><small>${e(x.time||'')}</small></div>`).join(''):`<div class="empty-state"><b>No 2026 waiver activity yet.</b></div>`;return `<div class="live-status"><span class="status-dot ${(w.apiStatus||Y.apiStatus||'').toLowerCase().includes('connected')?'on':'off'}"></span><div><b>${e(w.apiStatus||Y.apiStatus)}</b><small>Last update: ${e(w.lastUpdated||Y.lastUpdated)}</small></div></div><div class="waiver-grid">${card('Live-ready architecture',`The Makers now uses the same one-source setup as Misc.Exp. A secure Yahoo endpoint can refresh this page without putting credentials in GitHub.`,'API READY')}${card('Privacy rule',`Completed claims and FAAB spend can be public. Pending bids stay private until Yahoo processes them.`,'NO CHEATING')}</div><div class="waiver-section"><div class="section-head"><div><div class="kicker">MONEY LEFT TO BURN</div><h2>FAAB & waiver priority</h2></div></div><div class="table-wrap"><table><thead><tr><th>Priority</th><th>Manager</th><th>Team</th><th>Spent</th><th>Remaining</th><th>Claims Won</th></tr></thead><tbody>${faab}</tbody></table></div></div><div class="waiver-section"><div class="section-head"><div><div class="kicker">WHO IS STILL OUT THERE?</div><h2>Top available players</h2></div></div><div class="table-wrap"><table><thead><tr><th>#</th><th>Player</th><th>Pos</th><th>NFL</th><th>Status</th><th>Suggested FAAB</th></tr></thead><tbody>${avail}</tbody></table></div></div><div class="waiver-section"><div class="section-head"><div><div class="kicker">THE TAPE</div><h2>League activity</h2></div></div><div class="activity-feed">${tx}</div></div><div class="notice"><b>How updates work:</b><ul>${(Y.waiverNotes||[]).map(n=>`<li>${e(n)}</li>`).join('')}</ul></div>`}
function waivers(){return hero('Waiver Wire','FAAB, priority, available players and completed activity — all ready for the Yahoo bridge.')+`<section class="section"><div class="wrap"><div id="waiver-live-content">${waiverContent({})}</div></div></section>`}
async function refreshWaivers(){const cfg=window.MAKERS_API||{},url=cfg.waiverEndpoint;if(!url)return;try{let r=await fetch(url,{cache:'no-store'});if(!r.ok)throw new Error('HTTP '+r.status);let data=await r.json();data.apiStatus=data.apiStatus||'Connected to Yahoo';data.lastUpdated=data.lastUpdated||new Date().toLocaleString();let el=document.getElementById('waiver-live-content');if(el)el.innerHTML=waiverContent(data)}catch(err){let b=document.querySelector('.live-status b');if(b)b.textContent='Yahoo connection temporarily unavailable'}}
function schedule(){let rows='';for(let w=1;w<=14;w++){let games=(D.schedule2026[String(w)]||[]).map(x=>`${e(x[0])} vs ${e(x[1])}`).join(' · ');rows+=`<tr><td><b>${w}</b></td><td>${games}</td><td><a href="week.html?week=${w}">Open Week ${w} →</a></td></tr>`}rows+=`<tr><td><b>15–17</b></td><td>Championship and consolation brackets TBD</td><td><a href="weeks.html">Weekly hub →</a></td></tr>`;return hero('2026 Schedule','Fourteen regular-season weeks, then three weeks to settle the trophy and the shame.')+sec('Master schedule',`<div class="table-wrap"><table><thead><tr><th>Week</th><th>Matchups</th><th>Archive</th></tr></thead><tbody>${rows}</tbody></table></div>`,'Every regular-season matchup links directly to the shared Week engine.','THE ROAD MAP')}
function updatedRecords(){return D.records.map(r=>{let cur=Y.standings.find(s=>s.manager===r.manager);if(!cur)return {...r};let w=r.w+(cur.w||0),l=r.l+(cur.l||0),pf=r.pf+(cur.pf||0),pa=r.pa+(cur.pa||0),moves=r.moves+(cur.moves||0);return {...r,w,l,pf,pa,moves,pct:w/(w+l||1)}})}
function records(){let rs=updatedRecords().sort((a,b)=>b.w-a.w||b.pct-a.pct);let rows=rs.map((r,i)=>`<tr><td>${i+1}</td><td><b>${e(r.manager)}</b></td><td>${r.w}-${r.l}</td><td>${(r.pct*100).toFixed(1)}%</td><td>${Number(r.pf).toFixed(2)}</td><td>${Number(r.pa).toFixed(2)}</td><td>${r.titles}</td><td>${r.finals}</td><td>${r.moves}</td></tr>`).join('');let wins=[...rs].sort((a,b)=>b.w-a.w)[0],pf=[...rs].sort((a,b)=>b.pf-a.pf)[0],moves=[...rs].sort((a,b)=>b.moves-a.moves)[0],titles=[...rs].sort((a,b)=>b.titles-a.titles)[0];return hero('Record Book','The historical ledger now rolls forward automatically with 2026 standings and moves.')+`<section class="section"><div class="wrap"><div class="metric-grid"><div class="metric"><b>${e(wins.manager)}</b><span>${wins.w} career wins</span></div><div class="metric"><b>${e(pf.manager)}</b><span>${Number(pf.pf).toFixed(2)} career PF</span></div><div class="metric"><b>${e(titles.manager)}</b><span>${titles.titles} championships</span></div><div class="metric"><b>${e(moves.manager)}</b><span>${moves.moves} career moves</span></div><div class="metric"><b>2022</b><span>history begins</span></div></div></div></section>`+sec('Career standings',`<div class="table-wrap"><table><thead><tr><th>#</th><th>Manager</th><th>W-L</th><th>Win%</th><th>PF</th><th>PA</th><th>Titles</th><th>Finals</th><th>Moves</th></tr></thead><tbody>${rows}</tbody></table></div>`,'Billy starts in 2025; El Rubio remains a separate 2022–24 franchise.','THE LEDGER')+sec('Playoff records',`<div class="grid three">${Object.entries(D.playoffWL||{}).sort((a,b)=>b[1][0]-a[1][0]).map(([m,v])=>card(m,`<b>${v[0]}-${v[1]}</b> postseason record`)).join('')}</div>`)}
function franchises(){let rows=D.franchises.map(f=>{let r=historicalRecord(f.manager),t=ourTeam(f.manager);return `<a class="card franchise" href="franchise.html?manager=${encodeURIComponent(f.manager)}"><div class="kicker">${e(f.manager)} · SINCE ${f.since}</div><h3>${e(f.current)}</h3><div class="stat">${r?`${r.w}-${r.l}`:'—'}</div><div class="small">${r?`${(r.pct*100).toFixed(1)}% · ${r.titles} titles · ${r.moves} moves`:''}</div>${t?`<p><b>2026:</b> Our ${e(t.grade)} · rank #${t.rank}</p>`:''}${f.note?`<p class="small">${e(f.note)}</p>`:''}</a>`}).join('');return hero('Franchise Files','Every manager keeps his own receipts. Billy and El Rubio are separate histories.')+sec('Know your enemy',`<div class="grid two">${rows}</div>`)}
function franchise(){let m=Q.get('manager')||'Andrew',f=currentFranchise(m),r=historicalRecord(m),t=ourTeam(m);if(!f)return hero('Franchise not found');let aliases=(f.aliases||[]).map(x=>`<li>${e(x)}</li>`).join('');let base=`<section class="section"><div class="wrap profile-top"><div class="profile-box"><div class="big-record">${r?`${r.w}-${r.l}`:'—'}</div><p>${r?`${(r.pct*100).toFixed(1)}% career win rate · ${Number(r.pf).toFixed(2)} PF · ${r.moves} moves`:''}</p><ul>${aliases}</ul>${f.note?`<div class="notice">${e(f.note)}</div>`:''}</div>${t?`<div class="callout"><div class="kicker">2026 POWER #${t.rank}</div><h3>${e(t.team)} · ${e(t.grade)}</h3><p>${e(t.outlook)}</p></div>`:''}</div></section>`;let outlook=t?sec('2026 outlook',`<div class="grid two">${card('The good',e(t.best))}${card('How it goes wrong',e(t.concern))}</div>`,'','OUR SCOUTING REPORT')+sec('Draft snapshot',`<div class="grid three">${card('Our grade',`<span class="grade ${gradeClass(t.grade)}">${e(t.grade)}</span>`)}${card('Yahoo grade',`<span class="grade ${gradeClass(t.yahoo)}">${e(t.yahoo)}</span>`)}${card('Best value',e(t.bestValue))}</div>`):'';return hero(`${m} — ${f.current}`,`Franchise history since ${f.since}.`)+base+outlook}
function warroom(){let ourRows=Y.draftRankings.map(t=>`<tr><td>${t.rank}</td><td><b>${e(t.team)}</b><div class="small">${e(t.manager)}</div></td><td><span class="grade ${gradeClass(t.grade)}">${e(t.grade)}</span></td><td>${e(t.yahoo)}</td><td>${e(t.bestValue)}</td></tr>`).join('');let yahoo=[...D.currentTeams].sort((a,b)=>b[4]-a[4]).map((r,i)=>`<tr><td>${i+1}</td><td><b>${e(r[0])}</b><div class="small">${e(r[1])}</div></td><td>${e(r[2])}</td><td>${e(r[3])}</td><td>${Number(r[4]).toFixed(2)}</td><td>$100</td><td>${e(D.waiverPriority2026[r[1]])}</td></tr>`).join('');let settings=Object.entries(D.settings2026||{}).map(([k,v])=>card(k,e(v))).join('');return hero('2026 War Room','Our board and Yahoo’s board now sit side by side, with the schedule and live season architecture underneath.')+`<section class="section"><div class="wrap"><div class="metric-grid"><div class="metric"><b>471058</b><span>Yahoo league ID</span></div><div class="metric"><b>10</b><span>teams</span></div><div class="metric"><b>12</b><span>locked keepers</span></div><div class="metric"><b>$100</b><span>starting FAAB</span></div><div class="metric"><b>${Y.week}</b><span>current week</span></div></div></div></section>`+sec('Our draft-day field',`<div class="table-wrap"><table><thead><tr><th>#</th><th>Team</th><th>Our grade</th><th>Yahoo</th><th>Best value</th></tr></thead><tbody>${ourRows}</tbody></table></div>`,'This is the editorial board used by the homepage and Power page.','THE MAKERS VERDICT')+sec('Yahoo draft-day field',`<div class="table-wrap"><table><thead><tr><th>Power #</th><th>Team</th><th>Yahoo grade</th><th>Yahoo record</th><th>Power score</th><th>FAAB</th><th>Priority</th></tr></thead><tbody>${yahoo}</tbody></table></div>`,'Kept intact as a source snapshot for comparison.','YAHOO SNAPSHOT')+sec('League setup',`<div class="grid three">${settings}</div>`)+`<section class="section alt"><div class="wrap"><div class="hero-actions"><a class="btn primary" href="schedule.html">Open Full Schedule</a><a class="btn ghost darkghost" href="drafts.html">Our Draft Breakdown</a><a class="btn ghost darkghost" href="weeks.html">Weekly Room</a></div></div></section>`}
const routes={home,weeks,week,power,odds,transactions,waivers,schedule,records,franchises,franchise,warroom};
const page=document.body.dataset.page;document.body.innerHTML=nav()+(routes[page]||home)()+foot();
const hamb=document.querySelector('.hamb'),n=document.querySelector('.nav');if(hamb)hamb.addEventListener('click',()=>n.classList.toggle('open'));
const here=location.pathname.split('/').pop()||'index.html';document.querySelectorAll('.nav a').forEach(a=>{if(a.getAttribute('href')===here)a.classList.add('active')});
if(page==='waivers'){refreshWaivers();const cfg=window.MAKERS_API||{};if(cfg.waiverEndpoint)setInterval(refreshWaivers,Math.max(60,cfg.refreshSeconds||120)*1000)}
